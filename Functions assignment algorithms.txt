Functions algorithms:
I. 	1.Take the input of numbers
	2. Create functions max and min
	3.max(a,b,c):
		if a>b and a>c:
			sout(a)
		elif b>a and b>c:
			sout(b)
		else:
			sout(c)
	4.min(a,b,c):
		if a<b and a<c:
			sout(a)
		elif b<a and b<c:
			sout(b)
		else:
			sout(c)
		